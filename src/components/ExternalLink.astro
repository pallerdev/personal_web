---
interface Props { href: string; class?: string }
const { href, class: className } = Astro.props;
---
<a href={href} class={className} target="_blank" rel="noreferrer"><slot /><span class="sr-only"> (se abre en una pestaña nueva)</span></a>
